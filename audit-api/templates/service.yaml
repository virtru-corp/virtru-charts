apiVersion: v1
kind: Service
metadata:
  name: {{ include "audit-api.fullname" . }}
  labels:
    {{- include "audit-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "audit-api.selectorLabels" . | nindent 4 }}
