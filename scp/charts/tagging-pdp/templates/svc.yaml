apiVersion: v1
kind: Service
metadata:
  name: {{ include "taggingpdp.fullname" . }}
  labels:
    {{- include "taggingpdp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
      appProtocol: grpc
  selector:
    {{- include "taggingpdp.selectorLabels" . | nindent 4 }}