apiVersion: v1
kind: Service
metadata:
  name: {{ include "claims.fullname" . }}
  labels:
    {{- include "claims.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "claims.selectorLabels" . | nindent 4 }}
