apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "claims.fullname" . }}-test-connection"
  labels:
    {{- include "claims.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "claims.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
