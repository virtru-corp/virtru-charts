apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "key-access.fullname" . }}-test-connection"
  labels:
    {{- include "key-access.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "key-access.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
