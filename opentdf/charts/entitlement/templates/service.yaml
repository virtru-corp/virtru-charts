apiVersion: v1
kind: Service
metadata:
  name: {{ include "entitlement.fullname" . }}
  labels:
    {{- include "entitlement.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "entitlement.selectorLabels" . | nindent 4 }}
